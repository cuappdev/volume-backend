
/** 
 * Send IOS notification.
 */
const sendIOSNotification = async (userID: string, article: string, publication: string): Promise<void> => {
  // send notification to device token attached to this user id

};

/** 
 * Send Android notification.
 */
const sendAndroidNotification = async (userID: string, article: string, publication: string): Promise<void> => {
  // send notification to device token attached to this user id
};

/** 
 * Creates payload of a notification.
 */
const createPayload = async (userID: string, article: string, publication: string): Promise<void> => {
  // send notification to device token attached to this user id
  // have the payload 
};


export default {
  sendIOSNotification,
  sendAndroidNotification,
  createPayload
}