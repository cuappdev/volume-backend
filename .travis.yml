language: node_js
node_js:
- "14.15.4"

services:
- mongodb

install:
- npm install

before_script:
- npm run start

script:
- node_modules/.bin/newman run tests/volume.postman_collection.json -e tests/volume.postman_environment.json